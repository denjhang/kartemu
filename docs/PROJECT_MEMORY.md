# 项目记忆(Kartemu)

> 本文档是跨会话的"项目记忆":用户的硬性要求 + 当前正在做什么 + 已确认结论。
> 每次会话开始先读这里;新的重要结论必须写回这里。

## 用户硬性要求(不可违背)

1. **先逆向,后实现**:任何渲染/物理修复前,必须先把对应官方代码语义逆向清楚并写入
   UNDERSTOOD.md,禁止边猜边调。用户多次强调:"老老实实搞逆向"、"你先逆向工程,写入记忆"。
2. **代码是唯一事实来源**:禁止打开 KartSim 游戏做视觉对比("看代码是瞎猜?")。
   不查 CDN,所有代码本地都有。
3. **git 纪律**:自制工具必须全部开源入库(用户明确要求"涉及到的自制工具全部开源",
   打包成 exe 不开源是被批评的行为);kartemu 产物和官方 rho 资源绝不上传(mirror/、unpacked/、*.rho、
   deob_*.js、web/track|kart|character/ 等,见 .gitignore);只提交自研工具/文档/代码。
   完成一个阶段就 commit 并更新文档。
4. **禁止编造用户说过的话**。用户从未说"上下颠倒"。
5. KartRider-Tools 是闭源,不得参考;逆向靠自己。
6. 做完一个任务再做下一个(例如"先单独搞定人物渲染,再做赛车")。

## 总体目标

完整逆向 KartSim(kart.iii.moe),产出合法独立克隆 kartemu:
1 条赛道(城镇高速公路)、1 辆车(板车 cotton1)、1 个人物(皮蛋 dao),
运行时不接触 .rho 档案(使用 unpacked 资产导出物)。

## 当前任务(用户最新指令)

**kart.html 可玩版迭代中**(2026-09-14): 当前 = a8e9ff1 版(用户确认的"2分钟一圈"版本)。
**正确赛道标尺(已定案, 勿再反复)**: 官方城镇高速公路中心线圈长 **3,771 单位**(track.1s 实测),
路宽 20, kart 1.7 → 一圈 2,218 个车长, 极速 186km/h 下约 73 秒/圈 = 官方 1~2 分钟一圈。
对应 kart.html 版本 = a8e9ff1(真实路线图 + routeDist 围栏 + 标量物理封顶 51.7)。
**用户实测判定(2026-09-14)**: 此版地图正常 ✓; 漂移手感垃圾 ✗、轮胎印垃圾 ✗(待重做——
漂移重做方向: UNDERSTOOD §35 的 2D 轮胎力学(速度向量+抓地衰减)在 a8e9ff1 标量壳上失败的教训已录,
须整体替换物理层而非嫁接; 轮胎印须连续线段且贴地跟随路面)。
⚠️ 两个错误标尺不要再退回: 31,400u 放大程序化图(10分钟圈, 2a9926d, 用户否决)、31,820u(门截面点膨胀假值)。
⚠️ 2026-09-14 事故: 2D 矢量轮胎物理重写(04a1535)存在失控 bug(侧向抓地/阻力在合成环节失效,
直线加速无上限冲到 389km/h + 围栏弹回只翻标量不减速), 漂移/集气/连喷随之全部失真,
已按用户要求整体退回到 c433a01 版(标量物理+阻力上限 51.7m/s, 围栏穿越检测, 均正常)。
教训: ①矢量物理与围栏/弹回逻辑强耦合, 不能在标量壳上逐块嫁接; ②每改一处物理必须跑完整
加速-极高速-撞栏曲线再交付。漂移/集气/喷气/音效的官方语义逆向文档仍有效
(REV_COMPARE §二/§三, UNDERSTOOD §35 保留作设计参考), 重做时以 2D 轮胎力学一步到位。

**恢复原版 kart.html 可用状态** —— 已完成(2026-09-12, commit c6f7ce8/0ce2c8e):
黑屏事故已修复(见下节复盘),原版手调完全体(板车+皮蛋+挂件)恢复正常渲染,
three.js 已全部本地化,运行时零 CDN。

**背景**:2026-09-12 凌晨的 ES module 化迁移(kart.html 重构 + web/runtime/ 模块)
因依赖链断裂全部回滚(git checkout 恢复 kart.html,删除 web/runtime/ 等)。
回滚后发生黑屏事故(见下节)。迁移任务本身待环境稳定后**增量**重做
(教训:逆向成果应逐模块合入,不可一次性重写)。

**下一步**:按 FUNCTIONS.md 补齐剩余函数签名(台式机),或增量重启物理/赛道迁移。

## 2026-09-12 黑屏事故复盘(完整经过)

**现象**:回滚迁移代码后,用户打开 kart.html 报"原版也没有任何模型"——纯色背景,
无 3D 内容,UI 停在"加载中…"。用户认为 AI 把原版代码搞坏了。

**排查过程(全部实锤)**:
1. `git status` 确认 kart.html 与提交版本**完全一致**——代码没坏;
2. 资产完好:dao.gltf 内 char_root/f00-f54 全部动画在位,kart.gltf 内 ReKart/zup_root 在位;
3. 服务器 curl 测试 5 个 GLTF + bin 全部 200;
4. 浏览器实测复现关键差异:**同一文件,`localhost:8000/kart.html` 正常,
   `127.0.0.1:8000/kart.html`(纯 URL 无参数)黑屏**。

**根因(三层叠加,缺一不可)**:
1. **浏览器磁盘缓存毒化**:Chrome 缓存按源(origin)分区,localhost 与 127.0.0.1
   是两个独立分区。`127.0.0.1:8000/kart.html` 纯 URL 命中了早期实验页面的缓存
   (铁证:截图里出现了当前文件中根本不存在的"官方状态机(HB 11剪辑)"下拉框
   和"首次按键启动音效"提示——那是已回滚的实验版 UI)。
   纯导航/带 no-cache 头/F5 均无法击穿该缓存条目。
2. **CDN 挂起**:旧缓存页面的 importmap 从 jsdelivr 加载 three.js,国内网络下
   请求挂起 → `import` 永不返回 → 整个 module 不执行 → 停在"加载中…",
   `window.__scene` 为 undefined,什么都渲染不出来。
3. **原版代码隐患**:rAF 循环先于模型加载启动;模型加载失败时 kart.html:326
   每帧抛 `TypeError: window.__setCharAnim is not a function`,把同一循环里
   后面的 `renderer.render()` 拦断 → 连网格背景都不画(黑屏且无任何提示)。
4. **次要因素**:遗留的单线程 `serve.py`(http.server,8:41 启动占用 8000 端口)
   在多标签页并发请求时偶发 `net::ERR_ABORTED`,加重了加载失败概率。

**修复(commit c6f7ce8)**:
- three.js 0.178.0 全套本地化到 `web/lib/`(three.module.js/three.core.js/
  OrbitControls/GLTFLoader/BufferGeometryUtils 共 5 文件);
- kart/index/character.html 的 importmap 改指向 `./lib/three/`(逻辑零改动);
- kart.html 动画状态机加一行加载保护(`&& window.__setCharAnim`),
  模型未就绪时跳过状态机,不再拦断渲染;加载成功后行为完全不变;
- 新 `web/serve.py`:ThreadingHTTPServer(多线程)+ no-store 响应头,端口 8000,
  替换遗留单线程服务,杜绝并发 ERR_ABORTED 与缓存残留;
- 删除遗留空文件 kart_new.html。
- `py_compile` + `node --check` 语法检查通过。

**验证结果**:`http://localhost:8000/kart.html` → "完全体就绪",板车+皮蛋+
头戴摄像机+风镜+气球全部渲染(截图确认;画面中彩虹圆柱是原版代码里
MeshNormalMaterial 的碰撞测试障碍柱,非故障);index.html 赛道页正常;
网络请求全部本地,零 CDN。

**遗留注意事项**:
- **今后统一用 `http://localhost:8000/kart.html`**(localhost 分区缓存干净);
- `127.0.0.1:8000/kart.html` 纯 URL 的毒缓存条目仍在浏览器里(自动化工具无法
  强刷);如必须用 127.0.0.1,手动按一次 **Ctrl+Shift+R** 即永久修复,
  之后 serve.py 的 no-store 头保证不再被缓存;
- trae-preview 注入的 `/@vite/client` 404 无害,可忽略。

## 人物渲染当前状态(截至 2026-09-10)

- `char_gltf.py`:骨骼蒙皮 glTF 导出器(24 骨骼节点 + skin + 32 个动画剪辑)。
  - **已数值验证**:rest 局部矩阵 = inv(world_rest[parent]) @ inv(inverseBind);
    蒙皮语义与官方 skin=world(pose)@inverseBind 完全一致(f45@100ms 顶点偏差 0;
    页面运行时对拍 X/Z 差 <1mm)。
  - **JOINTS_0/WEIGHTS_0 必须 VEC4**(VEC2 会让 three.js 读到 undefined 关节)。
  - localBind 与 inverseBind **不自洽**(|bind_world@ib - I| 最大 2.0),官方
    从不用 localBind 链做绑定,逆向结论:弃用 localBind。
  - 刚性件挂接: local = 纯 m_walked(官方 world[bone]×local;YT.update 返回 world 数组,非 skin)。
  - 根节点 zup_root RotX(-90°): 独立渲染必须有;赛车内挂载时应去掉(父级已有)。
- `web/character.html`:独立查看页(OrbitControls + AnimationMixer + 32 动画按钮,
  自动取景)。当前 body 渲染正确,face 漂浮。
- `s1_gltf.py --character`:旧的单帧 f45@100ms 烘焙导出(曾用于 index.html 骑乘,
  视觉正确),保留作参照。

## 关键逆向结论速查(详见 UNDERSTOOD.md)

- 三空间:D(数据 Z-up)、P(物理=ae(D)=(x,z,-y))、轮廓(u1=Rx(+90°)·world)。
- 赛道/车辆根 rotation.x=-π/2;人物 model.1s 本身 Y-up,**不加旋转**
  (convertClientCoordinates:false)。
- cull:1=双面 2=CW正面 3=CCW背面;det<0 世界矩阵翻面。
- UV:官方 flipY=false + v 原样;glTF 同为左上原点,**不翻 v**。
- alphaTest 只认 AlphaProperty.alphaTestEnable;cotton1 0.png 的 alpha 是涂装遮罩。
- CharSequence:24 通道 PRS;rotation 记录 20B,文件序 (w,x,y,z);线性插值;
  官方**始终播放动画**,缺省通道 pos=0/rot=identity。
- 官方蒙皮:v' = w0·skin[bone0]·v + (1-w0)·skin[bone1]·v,bone1=255 表示无。
- 官方刚性挂接(§18 早先结论,待本次复核):face/head→bone5,handL→9,handR→14。
- 棉花糖骨架:0=seat,1=handle,2/3=前轮,4/5=后轮,6=人物挂点(0,-0.067,0.349),7=port0。
- 转向:右手系 (left-right) 输入;+yaw 朝屏幕左。
- .rho/.rho5 解密已验证(P236 RhoCrypto 对拍一致)。

## 待办队列

0. [新] 深度逆向对照完成(2026-09-13): docs/REV_COMPARE.md, 动画映射/补间、物理/漂移/集气/喷气、音频、速度表/仪表四方向,
   含总差异速查表 + 迁移顺序建议(动画映射修正 -> 力阻力物理 -> 速度表 -> 漂移集气 -> 喷气族 -> 音频)。
   后续实现按该顺序逐模块合入, 每步跑通 kart.html。
0b. [新] 函数级全量吃透完成(2026-09-13): docs/FUNCTIONS_DETAIL.md, FUNCTIONS.md 全部 📌/⏳ 补齐为 ✅
   (物理剩余/碰撞赛道剩余/输入/道具/音效/AI/渲染/资源解析/数学库/顶层索引), 含重要勘误:
   §I 旧行号全部作废(旧 dump 遗留), §E 键位表更正(DirectInput 扫描码), §H 无 AI(仅路线追踪+autoForward)。

1. [已完成] 人物单独渲染(char_gltf.py + character.html, §20)。
2. [已完成] 全方位逆向(§25-§32, 2026-09-11):物理引擎/碰撞系统/赛道构建/AI/比赛流程UI/道具/输入/音效。
3. [已完成] 函数级交接文档(2026-09-11):`docs/FUNCTIONS.md`,核心模块已详述,剩余模块列行号索引。
4. [已完成,后回滚] 2026-09-12 凌晨曾一次性迁移到 web/runtime/ 模块
   (physics/char_anim/input/sound/碰撞等),因依赖链断裂整体回滚。
   **重做时必须逐模块增量合入**:每合一个模块跑通 kart.html 再合下一个。
   工具 `tools/export_route.py`(赛道 ToRoad 路线导出, commit 0ce2c8e)已保留入库。
5. 广告牌背面实心橙(cull 方向/alpha)。
6. 碰撞重做(官方 Z$ 4m 均匀网格 rayQuery + 墙障碍, §26 已逆向, 待实现)。
7. 物理引擎实现(§25 已逆向, 待用 JS 重新实现: stepSubstep/applyLongitudinal/applySteeringAndTires)。
8. 比赛流程实现(§29 已逆向: raceLifecycle 状态机 + HUD + 相机)。
9. 可选:官方 body 贴图合成(Fw/Dw,§20.6 已完整逆向算法);车辆轮子动画。
10. 把 index.html 的骑乘人物换成新的骨骼蒙皮版(挂到赛车子树时去掉 zup_root 旋转)。
11. [已完成] KartRider-Tools v1.2.2 解包(§21)。
12. (台式机继续)补齐 FUNCTIONS.md 中 📌 / ⏳ 标记的函数签名,按行号在 deob_named.js 查找。

## 目录布局(2026-09-10 起)

- 自研工具全部在 `tools/`(15 个 .py);文档在 `docs/`;前端在 `web/`。
- `tools/server.py` 的 ROOT 已改为**项目根**(相对自身上两级),从任何位置启动都行。
- README 已匿名化且**不再包含维护备注**(代理等运维信息只记录在本文件)。

## 基础设施备注(2026-09-10)

- **GitHub 推送**:`github.com:443` 直连超时,本机 `127.0.0.1:7892` 代理可用:
  `git -c http.proxy=http://127.0.0.1:7892 push origin main`(未写入全局配置)。
  远端 https://github.com/denjhang/kartemu.git。
- **大文件清理(已完成)**:早期 commit 曾带入 mirror/*.rho 等官方资源(约 1.2GB),
  已用 `git filter-branch --index-filter` 重写全部历史剔除
  (mirror/ unpacked/ kartspec.csv deob_full.js aria2c.exe archive-index.*
  web/track|kart|character),reflog expire + gc 后仓库 352KB。
  **教训:任何官方资产在 add 前必须先查 .gitignore 是否覆盖**。
- 本地静态服务(两套,用途不同):
  - `python tools/server.py` → http://127.0.0.1:8088(官方镜像,/web/ 前缀映射项目 web/);
  - `python web/serve.py` → http://127.0.0.1:8000(web/ 开发服务,多线程+no-store;
    **用 localhost 打开**,127.0.0.1 有历史毒缓存,见黑屏事故复盘)。
- three.js 已本地化:`web/lib/three/`(0.178.0),页面 importmap 一律指向
  `./lib/three/`,禁止再引 CDN。

## 交付检查流程(硬性, 2026-09-10 两次白屏事故 + 2026-09-12 黑屏事故后)

页面类改动完成后必须依次通过, 缺一不可:
1. `node --input-type=module --check`(语法);
2. 浏览器打开后 evaluate 冒烟: `__scene/info 文本/canvas 数量` 正常;
3. 模拟一次真实交互(按键/点击)再截图确认渲染循环没死。
常见根因:
- 动画循环引用了 .then 回调内块级作用域变量(加载完成前每帧 ReferenceError);
- rAF 循环先于异步加载启动, 加载失败时每帧 TypeError 拦断 renderer.render()
  → 黑屏无提示。循环内对加载产物(如 __setCharAnim)必须先判存在;
- 浏览器缓存毒化: 验证时**必须带 cache-buster 参数或用 localhost 源**,
  否则测到的可能是旧缓存页面(2026-09-12 事故:127.0.0.1 纯 URL 命中
  早期实验页缓存, 页面从 CDN 拉 three.js 挂起, 黑屏误判为"代码坏了")。

## 2026-09-13 物理参数重大勘误(speedType) + 漂移/转向手感轮

- **kartspec.csv 每个 id 有两行: speedType=4 与 speedType=7, 字段几乎全同,
  唯一关键区别是 driftMaxGauge: speedType=4 → 1(一次漂移即满, 特殊模式),
  speedType=7 → 4300(正常竞速慢速集气)**。此前一直抓的 speedType=4 是错的,
  导致"1 次漂移=1 条槽"的错误结论(用户纠正: kartemu 是正常竞速模式)。
  **以后取 cotton1 参数一律用 speedType=7 行**。c1/speedType=7 关键值:
  driftMaxGauge=4300, speedSlotCapacity=2, driftSlipFactor=0.2, driftTrigTime=0.2,
  cornerDrawFactor=0.18, boostAccelFactor=1.494, normalBoosterTime=3000。
- 集气已改为官方慢速公式(accumulateDriftGauge L24365): pending += vl²·dt,
  权重前0.2s×3/0.5s内×1.5/之后÷(2t), 上限 4300, commit 后清零转 N2O 入槽。
  vl=侧向速度(米/秒)。最佳化漂移(大侧滑)单次约 +700, 与"5-6 次集满"吻合。
- **转向已换成官方动态轮胎力模型**(不再是稳态捷径/恒定角速度):
  前后轮滑移角 δ∓vr∓wT, 力=ue(980)·G(5)·滑移角, 侧向 vl̇=(Ff+Fr)/m,
  横摆 ω̇=Pr(Ff−Fr)/(m/12)(L24354 惯量=mass/12), δ=steer·10°·exp(−|v|/22.25),
  cornerDraw(0.18) 转弯拖拽。常数 L23407(Pr=0.5)/L23409(ue=980)/L24859(expScale=1)。
  **必须子步积分**(官方 L23845 dt≤0.002s; 实测 4ms 稳定, 16ms 数值发散会抽干速度)。
- OFFICIAL 常量表已提升到 kart.html 模块顶层(updateTacho 也引用;
  此前声明在渲染循环内导致每帧 ReferenceError→画面全黑, 已修)。
- 最佳化漂移: Shift 按下沿触发(L17212), 轻点后松开仍走完整漂移(触发0.2s+滑行0.4s,
  滑行横摆加强到 0.45 保持弧线); 集气窗在触发期结束打开。
- 待办: 用户实测转向手感(满舵高速侧滑+cornerDraw 减速很猛属模型真实行为, 若偏离
  官方手感再查轮胎饱和/抓地表 _s)。

## 2026-09-13 续: 集气标定 + 轴距修正
- 集气单位标定: 官方内速单位未知(公式 vl²·dt 原样保留), 归一系数 k=2.8 按
  "c1 5-6 次最佳化漂移集满 4300"用户基准标定。实测 150km/h 强漂移单次 +0.19~0.21
  (即 1/5 条), 集气天然随车速²增长(用户要求的"集气和车速挂钩"由此成立)。
- 轴距修正: LocalTimeAttackParameters 官方参数行内含 wheelBase=1.2999999,
  转向动态模型力臂 halfWheelbase 0.5 -> 0.65(角速度 -30%)。此前用 2×Pr=1.0 偏小。

## 2026-09-13 续2: 最佳化漂移反拉机制 + 黄油衰减 + 轮胎饱和 + 网格闪烁
- **漂移反拉结束机制(用户多次指出后落地)**: 漂移不由 Shift 维持(松开只进 0.4s 滑行);
  结束条件 = 反向拉车头把滑角拉过零。实现: steerMod=1+steer·dir·1.5(反拉系数>1,
  车头会被拉回并可越过速度方向); hadSlip 锁存(滑角>0.12rad 才允许反拉结束,
  防止上次残留负滑角误杀新漂移——此前集气 0.00 的元凶); 过零时 commit 集气+开 B50 小喷窗。
- **黄油衰减**: 官方漂移力×0.2 后滑移功率≈0.1m/s², 此前 v·ω²/1.8 达 17m/s²(170倍)。
  旧路径改 0.15m/s² 常量; 矢量分支漂移抓地 1.8->0.25(vl 长存)。
- **轮胎滑移饱和**: 线性轮胎模型无上限导致抓地满舵 5g 过弯(角速度过大元因)。
  θ_sat=0.10rad(≈5.7°) 标定: 抓地满舵 a_max≈9.8m/s², 172km/h 半径≈180m(迟钝, 不减速必撞墙)。
- **网格远处闪烁**: 世界坐标 ±5000 下 fract 精度崩坏; 网格改用相机吸附相对坐标
  (floor(cam/2)*2 偏移, 数学界完全一致)。漂移痕迹远处被路面深度抢占: polygonOffset -4 + 抬 0.05m。

## 2026-09-13 续4: 两阶段漂移已退回(9271e9b)
- 用户报告默认行走被"不知名的力量打滑"。根因: velDir 硬跟随 lerp 删除后,
  低速时力模型 velDir 旋转率 = a_lat/v, v 小则角速度爆炸(除以小速度),
  且冰面漂移冻结 velDir 与回正衔接不稳。
- 教训: 力模型 velDir 积分必须做低速保护(v 下限/低速切回运动学对齐),
  两阶段方案需重新设计后再上。

## 2026-09-13 续5: 漂移模型重思考(用户模型落地)
- **用户模型**: "漂移行为 ≈ 松油门抓地过弯(减速感), + 丧失部分抓地力(= _s 表 dirt/slip
  打滑语义), + 集气/小喷"。漂移不再有独立高横摆率(突变角速度元凶), 转向与抓地共用
  同一套迟钝动态力模型; velDir 跟随率 漂移1.8 / 抓地10(= 丧失部分抓地)。
- **速度衰减**: 漂移中推力×0.5("半油门"), 加上阻力+cornerDraw → 172km/h 漂0.8s
  ≈ -21km/h(实测), 无固定衰减常数。
- 两阶段"冰面"方案(4a36785)已退回: velDir 力积分在低速除以小速度发散 → 行走打滑。
  教训: 力积分需低速保护, 待重设计。
- 集气 k 重标定: 转向迟钝化后平衡滑角变小, k 2.8 -> 11.2(保持 5-8 次集满的用户基准)。
- 未竟事项: _s 路面表(dirt/slip/雪地冰库)的完整逆向与打滑地面复用; 小喷 1.494 官方值
  在新漂移衰减下重新感受。

## 2026-09-13 续6: 漂移分量模型(用户机制最终落地)
- **机制**: Shift 按下一瞬冻结速度分量; 前向分量随按住衰减(8%/s), 侧向分量(localRightSpeed)
  保持(0.3/s 轻微损耗); 行进方向 = 车头 - atan2(vl, vf)。
  → 点按 = 缓弯衰减少; 长按 = 前向小转弯急(U型弯) —— 转弯快慢来自前向衰减, 不来自特殊转向。
- **集气**: vl²·dt 官方公式, 天然 ∝ 车速² 且随按住时长递减(权重 3/1.5/1/(2t));
  点按最佳化漂移单位时间集气最快; 慢速几乎不集气。实测 172km/h 点按 0.5s +0.22。
- **小喷阈值**: 只有甩出滑角(hadSlip>0.12rad)的漂移结束才开 B50 尾窗; 轻点没甩头不给。
- 转向: 漂移/抓地共用同一套迟钝动态力模型(漂移急缓来自分量模型, 不来自转向差异)。

## 2026-09-13 续7: 网格抖动/不可见根修(贴图平铺替代 shader fract)
- 根因: shader 内 fract 画网格线在世界坐标 ±5000 时 float 精度崩坏(闪烁+左右抖动),
  且淡出仅 150m 导致"大部分时间看不见"。
- 修复: 网格改硬件重复平铺贴图(RepeatWrapping CanvasTexture 128px=2m 格),
  uv 用相机吸附相对坐标(floor(cam/2)*2 偏移, 世界对齐不变), GPU 采样零精度抖动;
  可见距离 60→300m 淡出, 之后雾(200-400)接管。

## 2026-09-13 续8: 矢量模式移除 + 漂移双模式 + 外围贴图
- **移除新漂移(矢量轮胎模型)**: cb-vector/newphys 全部删除(用户判定不实用),
  加速/转向/移动/反弹的矢量分支一并清除, 只保留标量路径。
- **漂移双模式(下拉)**: legacy(旧版对照, 小喷 1.494×0.5s, 恒定 8%/s 衰减) /
  v2(新漂移2: 小喷 1.3×0.35s 减弱版, 按住递增衰减 8%+8%/s 上限 25%)。
  **灵敏度滑块**(0.5-2.0)乘在漂移横摆 steerMod 上。
- **赛道外围贴图**: 堤岸带复用网格贴图材质(此前纯色 0x4f9640 无贴图),
  网格平面下移 -0.05 防外缘 z-fight; 地面网格贴图去掉 60-300m 淡出(全图铺设, 雾管远距)。
- 重构事故记录: 清理矢量孤块时误删了整个官方加速块(boostMul/推力/阻力),
  已重建并按模式差异化小喷倍率; drifting 声明提升到循环顶部。

## 2026-09-15: SHIFT衰减滑块 + 模式3(越按越灵敏)
- SHIFT衰减滑块(0-0.20, 默认0.08): 漂移前向分量衰减率, 三模式通用;
  v2 在滑块值上额外按住递增(每秒+8%, 上限25%)。
- 模式3(v3): 漂移灵敏度随按住时长增长 sens = 漂移灵敏度 × min(3, 1+1.5×按住秒) ——
  点按低灵敏(缓), 长按高灵敏(渐急), 对应 U 型弯长按需求。
- 漂移模式下拉现有: legacy(对照) / v2(按住减速+弱小喷) / v3(越按越灵敏)。

## 2026-09-15: 赛道加宽一倍 + 漂移全滑行 + 撞墙方向恢复
- 赛道加宽: ROAD_HALF 10→20(路宽40), 围栏碰撞阈值随动(ROAD_HALF+0.4)。
- 漂移=全滑行: 漂移中推力×0(此前×0.5 造成 ~80km/h 的油滑平衡点, 按住 shift
  速度衰减不到 0——用户实测纠正)。按住 shift 速度持续衰减(shiftDecay 滑块
  +阻力), 松开才恢复推力。
- 撞墙方向恢复: bounceVel 里泄放横摆动量(×0.2)+消除侧滑(velDir 对齐车头),
  撞墙后立即可控。
- 官方极速核对: 显示速度 = |v|×3.6(L24750); cotton1 参数(推力2160/阻力0.75/3/质量100)
  代入官方阻力公式 → 极速 ≈51.7m/s ≈ 186km/h 显示值。公式推导如此, 若与官方游戏
  实测不符, 差异应在未抓到的 driveScale/路面 DF 表, 待后续。
- 补记: 加宽遗漏——堤岸带 OFFS 表写死 [10,20,35,60,100], 起点落在加宽后的路面下造成
  路缘高程断崖; 已改为随 ROAD_HALF 缩放([RH, RH+10, RH+25, RH+50, RH+90])。

## 2026-09-15: 快拉机制
- 漂移中(Shift按住+active阶段)反方向拉车头: 横摆上叠加反向漂移分量
  (baseSm×0.9×反打量×速度因子), 车身被快速拉正; 滑角过零后漂移正常结束
  (集气 commit + B50 小喷窗)。反打量 = min(1, -steer·dir)。

## 2026-09-15: 漂移改出阈值(可调)
- 改出条件从"滑角过零即改出"改为"反向滑角超过 -改出阈值 才改出"
  (阈值滑块 0.05-0.5rad, 默认 0.20, localStorage 持久化)。
- 动机: 过零即改出让轻点漂移滑角稍一回落就消失, 出不来最佳化漂移(用户判定)。
- 阈值调大 = 漂移更黏(反拉要求更深), 调小 = 更容易改出。
- 改出阈值语义修正(用户判定): 结束条件 = 滑角回落到阈值以下(slip < 阈值)。
  阈值小 = 难改出(漂移黏, 滑角收近归零才结束); 阈值大 = 易改出(滑角稍降即结束)。
  此前写成"反向超过-阈值"方向反了。

## 2026-09-15: 滑行时长语义(改出阈值的正确含义)
- 改出阈值 = 控制"点按 Shift 后的滑行时长": 时长 ≈ (松Shift时滑角 − 阈值) / 侧滑衰减率。
  阈值小 = 滑行久(难改出), 阈值大 = 滑行短(易改出)。
- 配套: 去掉 decay 0.4s 计时器硬掐(滑行被强杀=漂移快速消失, 不符官方);
  decay 横摆 0.45→0.2, velDir 跟随 decay 0.9/s(漂移中 1.8) —— 滑角缓慢自然衰减。
- decay 结束条件只剩: 滑角 < 阈值 或 速度 < 3。

## 2026-09-15: 无法改出根因修复
- 动态转向力模型此前无条件运行, 漂移/滑行期与启发式横摆**双重旋转车头**,
  滑角只增不减 → 永远无法改出(阈值滑块自然"没用")。
- 修复: 力模型仅在非漂移时运行; 漂移/滑行横摆由启发式独占
  (decay 0.2 缓摆 + 反拉快拉 + velDir 跟随 0.9), 滑角缓慢衰减至阈值改出。
- 平衡式: 滑角衰减率 ≈ velDir跟随(0.9) − decay横摆(≈0.2) ≈ 0.7/s,
  反打时更快; 无输入自然滑行约 0.4-0.6s 改出。

## 2026-09-15: 正拉延续/反拉加速改出(显式语义)
- 漂移改出判定加 pulling 守卫: steer·dir > 0.3(同方向维持拉) 时漂移强制延续,
  滑角再低也不改出; 反方向(反拉)则加速滑角收缩改出。
- 完整语义: 甩头建立滑角 → 正拉维持漂移 → 反拉加速改出(滑角 < 阈值时结束,
  commit 集气 + B50 小喷窗)。

## 2026-09-15: 集气满槽即刻发放
- 满槽(gauge+pending ≥ 4300)立刻获得 N2O 并清零, 不等漂移结束(用户判定)。
- k 重标定 3.7(分量模型下典型点按 vl≈16, 0.4s, w̄2 → 单次≈760 ≈ 4300/5.7)。
- 注: 8523a47 的 k=11.2 在 checkout 6aeddeb 时丢失, 本次按分量模型重标。

## 2026-09-15: 双胎迹收敛衰减
- 漂移衰减 = SHIFT衰减滑块 × 双胎迹收敛因子。每帧取两后轮触地点,
  收敛度 conv = (前帧印距-本帧印距)/轮印位移, [-1,1]:
  0=平行(缓滑), 正=靠近(急漂), 负=张开。
  因子: 收敛 ×(1+2conv) 最高×3; 张开 ×(1+0.5conv) 最低×0.5。
- v2 递增衰减在乘收敛因子前结算。

## 2026-09-15: 二次 Shift 泵送机制
- 漂移中(active)同方向再点 Shift: pumps+1(上限4), 漂移横摆灵敏度 ×(1+0.4×pumps)
  —— 正向漂移越点越重; 反方向再点走快拉(叠加反向横摆快速改出)。
- drift 开始时 pumps 清零。
- 二次Shift泵送幅度修正(用户判定"+40%效果不明显/语义应为隐约调整"):
  每层 +15%(上限4层 = ×1.6), 多次连点漂移横摆渐进加重。

## 2026-09-15: Shift加成 = 直接放大漂移灵敏度参数(用户定义)
- 漂移中同方向点一次 Shift: D.sensBonus += 0.3(上限+1.2);
  有效灵敏度 = 漂移灵敏度滑块值 × (1 + sensBonus) —— 即滑块调的那个参数等比放大。
- 清零: 改出时 / 漂移开始时 / 改方向(反打 steer·dir < -0.1)时。
- 泵送旧实现(+15%/层 pumps)删除。

## 2026-09-15: 加成双计数器(用户第3次纠正)
- 加成同时取决于 Shift 按键数量 与 同方向方向键点按数量:
  sensBonus = min(+120%, 30%×shiftN + 15%×dirN)。
  shiftN: 漂移中 Shift 按下沿(上限4); dirN: 同方向键按下沿(上限6)。
  反打(steer·dir<-0.1)清零; 漂移开始清零。HUD 显示两个计数。

## 2026-09-15: 漂移卡死(状态)修复
- decay 滑行期横摆 0.2 与 velDir 跟随 0.9 达到平衡滑角 ≈0.24rad > 默认改出阈值 0.20
  → 滑角永不达标 = 漂移状态卡死(不拉车头永远滑)。
- 修复: decay 横摆 0.2 → 0.1(平衡滑角 ≈0.11 < 任何合理阈值), 滑角必然缓慢收敛改出。
- 通用规则: decay 平衡滑角(横摆/跟随) 必须 < 改出阈值默认值, 调参时需联动检查。

## 2026-09-15: 加成可感知化(双计数+跟随率提升)
- 用户反馈"多次按shift/同方向拉车头没有大的转弯角速度提升": 根因 = 加成只乘车头横摆,
  行进方向跟随率恒 1.8, 路径转弯速度几乎不变 → 只有车身姿态甩、路径不收, 感知不到。
- 修复: ①双计数(shiftN 上限4 / dirN 同方向键按下沿上限6), 加成 = min(120%, 30%·shiftN + 15%·dirN);
  ②跟随率 = (decay 0.9 / 漂移 1.8) × (1 + 加成×0.6) —— 加成越高路径转弯越快, 可感知。
- 反打清零、hadSlip、漂移开始清零均保留。

## 2026-09-15: 加成三计数(HUD统计)
- 漂移中三计数: shiftN(Shift按下沿,上限4,每个+30%) / dirN(同方向键按下沿,上限6,每个+15%) /
  oppN(反方向键按下沿,仅统计不加成)。
- 加成 = min(+120%, 30%×shiftN + 15%×dirN); 反打清零; 漂移开始清零。
- HUD: "Shift: X 同向键: Y 反向键: Z / 加成: +N%"。
- 注: 仓库曾被重置回 7d1beed(双计数/持久化/加宽等提交丢失), 本轮基于该状态重做三计数。

## 2026-09-15: 加成=直接注入滑角(用户判定"滑角变小"根因)
- 此前加成同时放大 车头偏转 与 velDir跟随率, 两者平衡滑角几乎不变 → 滑角反而变小。
- 修复: 车头bonus偏转与跟随率加成删除; 加成在 velDir lerp 之后直接注入:
  velDir -= dir·sensBonus·0.5·min(1,|v|/15)·dt —— 行进方向向外(离心)偏转,
  滑角 = 基础 + 加成, 不可被跟随抵消; 轮胎印前方收敛/靠近。

## 2026-09-15: 空气墙修复 + 当前版本定版
- **空气墙根因**: 围栏跨越检测中 dPrev(旧帧距离)与 dNow(新帧距离)参照不同路段,
  弯道/近路段区域最近路段索引跳变 → 假"穿越" → 路中凭空弹墙。
  修复: 新增 distToSeg(点对指定路段垂直距离), 前后帧都对"当前最近路段"测量, 几何自洽。
- **当前可用定版**: 3949dcb 基线 + decay横摆0.1 + N2O抵消衰减 + 漂移期阻力豁免
  + SHIFT衰减0-0.40滑块 + HUD左栏 + 加成直接注入滑角(velDir离心偏转)。
- 已撤下: 配置持久化、赛道加宽、快拉、满槽即发、双胎迹收敛衰减、双计数器/调试面板
  (均因漂移页面卡死回退, 卡死根因待查: 疑似 cb165e8 双计数器提交)。

## 2026-09-15: 改出加成/低速门槛
- 改出加成: 小喷/大喷中改出阈值×2(喷气顶开滑角, 更易改出);
  速度越慢阈值越大: ×(1 + 1.5×max(0, 1 - v/20)), 低速(v→0)阈值×2.5 必改出。
- 起漂门槛: 车速 > 8m/s(≈29km/h) 才能起漂 —— 低速无法漂移(用户判定)。

## 2026-09-15: 小地图 + 坐标显示
- 右上角小地图(220x150 canvas): FR 路线帧等比缩放绘制中心线(路宽40m按比例),
  起点白标 + 自车红点每帧更新; 图下方显示 X/Z/Y 坐标(报错定位用)。

## 2026-09-15: 改出加成改为"点燃瞬间窗口"
- 小喷点燃: 改出阈值×1.5; 大喷点燃: ×2 —— 只在点燃后 0.4s 窗口内有效
  (drive.exitBoostUntil/exitBoostMul), 喷气状态持续期间漂移无改出加成。
- 取代此前"state!=0 全程×2"的实现。

## 2026-09-15: 漂移抓地力滑块
- 漂移中 velDir 跟随率 = 漂移抓地力滑块(0.5-3.0, 默认1.8 = 官方 driftSlipFactor 0.2 的部分抓地);
  decay 滑行期 ×0.5。滑块大 = 漂移黏/弧线紧, 小 = 冰面滑。
- 已并入配置持久化(kartemu_cfg_v1)。

## 2026-09-15: 立交路段空气墙/轮胎印消失修复(高程感知)
- 用户坐标 (346,-326,10.8) 处空气墙 + 局部无轮胎印: routeNearest 仅按 XZ 找最近路段,
  立交/桥梁上下两层俯视重叠 → 最近路段层间跳变 → 围栏误弹+地面高程跳变(轮胎印画错层)。
- 修复: routeNearest/routeDist 加高程权重(dy×8 入距离平方), 调用点传入 p.y —— 同层严格优先。

## 2026-09-15: 小地图原版样式 + 坐标瞬移
- 小地图改原版风格: 深底 + 灰色赛道路面(#9aa0a6)+深灰描边 + 黄色三角形标明车头方向
  (rotate(π - heading) 对齐车头向量)。
- 坐标行双击 → 输入框(X, Z) → 回车瞬移(重置 routeHint 全局搜索, 同步 prevX/prevZ 防误判围栏),
  Esc 取消。
